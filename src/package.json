{
  "dependencies": {
    "axios": "^1.4.0",
    "copyfiles": "^2.4.1",
    "dotenv": "^16.0.3",
    "rimraf": "^5.0.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "prebuild": "rimraf ../dist",
    "build": "tsc",
    "postbuild": "copyfiles package*.json ../dist/ && cd ../dist && npm ci --production"
  }
}
